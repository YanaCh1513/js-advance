var app = new Vue({
    el: "#app",
    data: {
        filterText: ''
    },
    method: {
        changesearch(text) {
            console.log('sfsdfsdf' + text);
        }
        // test(event) {
        //     console.log(event)
        // }
    },
});